class Warplan {
    constructor(player, warState, baseCommand, unitCommands) {
        this.player = player;
        this.warState = warState;
        this.baseCommand = baseCommand;
        this.unitCommands = unitCommands;
    }
}

export default Warplan;